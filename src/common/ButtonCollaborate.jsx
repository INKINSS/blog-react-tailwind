import React from "react";

const ButtonCollaborate = ({color}) => {
  return <button className={`border-2 border-${color} text-${color} text-[1.2rem] px-5 py-2 mt-5`}>colabora</button>;
};

export default ButtonCollaborate;
