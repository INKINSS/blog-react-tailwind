import React from "react";

const ButtonDiscover = ({color}) => {
  return <button className={`border-2 border-${color} text-${color} text-[1.2rem] px-5 py-2 mt-5`}>descubre</button>;
};

export default ButtonDiscover;
